- name: install Packages
  hosts: local
  connection: local
  become: yes
  tasks:
    - name: packages
      ansible.builtin.Package:
        name: "{{ item }}"
        state: latest
      loop:
      - nginx
      - mysql
      - zip
      - nodejs