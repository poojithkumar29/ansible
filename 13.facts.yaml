- name: Using facts
  hosts: local
  connection: local
  become: yes
  tasks:
    - name:  install nginx
      ansible.builtin.dnf:
        name: nginx
        sate: present
      when: ansible_os_family == "RedHat"
    - name:  install nginx
      ansible.builtin.apt:
        name: nginx
        sate: present
      when: ansible_os_family == "RedHat"

