- name: Functions
  hosts: local
  connection: local
  vars:
    technologies: devops,aws,shell,ansible
  tasks: 
    - name: Default variable
      ansible.builtin.debug:
        msg: "Hello {{ name | default('poojith') }}"
    
    - name: string to list
      ansible.builtin.debug:
        msg: "The list is :{{ technologies | split(',') }}"


    

